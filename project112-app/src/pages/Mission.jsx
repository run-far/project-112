import {useApp} from '../context/AppContext';import {Card,PageTitle,Metric} from '../components/UI';import {daysUntil,fmtDate} from '../utils/format';export default function Mission(){const {state}=useApp();const total=state.activities.reduce((s,a)=>s+a.distance,0);return <><PageTitle eyebrow="Mission Control" title={state.mission.name}/><div className="grid"><Card className="hero wide"><div className="hero-stats"><Metric label="Ziel" value={`${state.mission.targetKm} km`}/><Metric label="Countdown" value={`${daysUntil(state.mission.date)} Tage`} sub={fmtDate(state.mission.date)}/><Metric label="Trainings-km" value={`${total.toFixed(0)} km`}/></div></Card><Card><p className="eyebrow">Meilenstein</p><h2>{state.mission.milestone.name}</h2><p>{fmtDate(state.mission.milestone.date)} · noch {daysUntil(state.mission.milestone.date)} Tage</p></Card><Card><p className="eyebrow">Prinzip</p><h2>Finish stronger.</h2><p className="muted">Konstanz, verträgliche Belastung, getestete Verpflegung und ein belastbarer Trinkplan.</p></Card></div></>}
