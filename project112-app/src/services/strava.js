const api=import.meta.env.VITE_API_BASE_URL||'';
export function connectStrava(){const clientId=import.meta.env.VITE_STRAVA_CLIENT_ID;if(!clientId)throw new Error('VITE_STRAVA_CLIENT_ID fehlt');const redirect=import.meta.env.VITE_STRAVA_REDIRECT_URI||`${location.origin}${import.meta.env.BASE_URL}settings`;location.href=`https://www.strava.com/oauth/authorize?client_id=${clientId}&response_type=code&redirect_uri=${encodeURIComponent(redirect)}&approval_prompt=auto&scope=read,activity:read_all`}
export async function exchangeCode(code){if(!api)throw new Error('VITE_API_BASE_URL fehlt');const r=await fetch(`${api}/api/strava/exchange`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({code})});if(!r.ok)throw new Error('Token-Austausch fehlgeschlagen');return r.json()}
export async function fetchActivities(token){if(!api)throw new Error('VITE_API_BASE_URL fehlt');const r=await fetch(`${api}/api/strava/activities`,{headers:{authorization:`Bearer ${token}`}});if(!r.ok)throw new Error('Aktivitäten konnten nicht geladen werden');return r.json()}
export function mapStrava(a){return{id:String(a.id),name:a.name,date:a.start_date_local.slice(0,10),distance:+(a.distance/1000).toFixed(2),duration:Math.round(a.moving_time/60),elevation:Math.round(a.total_elevation_gain||0),avgHr:a.average_heartrate?Math.round(a.average_heartrate):null,type:a.type,temperature:null,source:'strava'}}
